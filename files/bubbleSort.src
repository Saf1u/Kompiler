$$$$$$$$$$$$$
   
/* print the array */
function printArray(arr: integer[9], size: integer) => void 
{
  localvar n: integer;
  localvar i: integer; 
  n = size;
  i = 0; 
  while (i<n) { 
      arr[i] = i;
         i = i+1;
  };
  i=0;
  while (i<n) { 
    write(arr[i]);
      i = i+1;
  };
} $$$$$$ !!!!`````

function main()=>void{
  localvar x:integer[9];
  printArray(x,9);
}
|||

